{
  "name": "studyhelper-e2e",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "codegen": "playwright codegen http://localhost:8081"
  },
  "devDependencies": {
    "@playwright/test": "^1.47.2"
  }
}

